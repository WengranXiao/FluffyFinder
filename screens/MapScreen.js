import { StyleSheet, Text, View, SafeAreaView } from "react-native";

const MapScreen = () => {
  return (
    <SafeAreaView>
      <Text>Map</Text>
    </SafeAreaView>
  );
};

export default MapScreen;
